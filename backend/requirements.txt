# Plately Backend — Python Dependencies
# ========================================

# --- Core Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-multipart>=0.0.9

# --- Database (Supabase) ---
supabase>=2.9.0
asyncpg>=0.30.0

# --- AI / Vision ---
# clarifai-grpc>=10.8.0  # Optional: heavy native dependency, install locally only
#   Railway/Docker: skip this, vision_detect uses Ollama fallback

# --- Computation ---
numpy>=2.1.0
google-generativeai>=0.8.0

# --- Utilities ---
python-dotenv>=1.0.1
httpx>=0.27.0

# --- Rate Limiting ---
slowapi>=0.1.9

# --- Monitoring (optional on free tier) ---
sentry-sdk[fastapi]>=2.0.0
structlog>=24.1.0
prometheus-fastapi-instrumentator>=7.0.0

# --- Testing (not needed in production) ---
# pytest>=8.3.0
# pytest-asyncio>=0.24.0
